- name: printing packages in Loops
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: print packages
    ansible.builtin.package:
      name : " {{ item }}"
      state: latest
    msg: " package installed: {{ item }} "
    loop:
    - nginx
    - nodejs
