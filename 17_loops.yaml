- name: Names in loop
  hosts: local
  connection: local

  tasks:
  - name: print names in loop
    ansible.builtin.debug:
      msg: " {{ item }} "
    loop:
    - suresh
    - ramesh
    - fivestar
    - add
